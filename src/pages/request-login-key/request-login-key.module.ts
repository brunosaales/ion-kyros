import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { RequestLoginKeyPage } from './request-login-key';

@NgModule({
  declarations: [
    RequestLoginKeyPage,
  ],
  imports: [
    IonicPageModule.forChild(RequestLoginKeyPage),
  ],
})
export class RequestLoginKeyPageModule {}
